# Test Development Stack
# Inherits from catalog/hello-world.yaml and overrides specific values
vars:
  tenant: local
  environment: test
  stage: dev

components:
  terraform:
    hello-world:
      vars:
        # Override defaults from catalog for dev environment
        container_name: "atmos-test-nginx"
        external_port: 8081